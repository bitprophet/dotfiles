case $( uname -s ) in
    Darwin )
        source $ZSH/mac ;;
    Linux )
        if test -f /etc/NIXOS; then
            source $ZSH/nixos
        else
            source $ZSH/linux
        fi
        ;;
esac
