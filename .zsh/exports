# General
export TERM="xterm-256color"
if [[ `uname -s` == *CYGWIN* ]]; then
    export TERM="cygwin"
fi
export DISPLAY=:0.0
export EDITOR=vim

# PATH
my_path=$HOME/bin
local_path=/usr/local/bin:/usr/local/sbin
export PATH=$local_path:$PATH:$my_path

# Homebrew temp folder
export HOMEBREW_TEMP=/usr/local/tmp
