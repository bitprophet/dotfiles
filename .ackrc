# Always page, because saying --color | less -R by hand every time is annoying
--pager=less -FXR
# Don't look in various build dirs when ack'ing please
--ignore-dir=htmlcov
--ignore-dir=build
# Ignore .coverage files
--ignore-file=is:.coverage
# Ignore pytest caches
--ignore-dir=.cache
--ignore-dir=.pytest_cache
# And mypy caches
--ignore-dir=.mypy_cache
